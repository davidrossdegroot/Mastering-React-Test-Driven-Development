import React from 'react';

interface Props {
  props: any;
}

export const AppointmentForm = (props: Props) => {
  return (
    <form id="appointment">
      <select name="service" id=""></select>
    </form>
  );
};
